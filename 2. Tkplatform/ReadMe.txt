2. Tkplatform 
It include two sub projects.
First one is generating all kinds of ratio and chart automatically, the main file is main.py, it will call Graph.py, rolling_data.py, and static_data.py to finish the calculation, also, three modules will be called at the same time.
Second one is a testing example which pulls the data of bitcoin, the main file is Tkinter.py

